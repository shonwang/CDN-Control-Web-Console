define("liveCnameSetup.view", ['require','exports', 'template', 'modal.view', 'utility', 'cnameSetup.view'], 
    function(require, exports, template, Modal, Utility, CnameSetupView) {

    var LiveCnameSetupView = CnameSetupView.extend({
        events: {},
    });

    return LiveCnameSetupView;
});